import { TextCSS, FontCSS } from "fjuttes";

export const SUMMARY_TEXT_CSS = new TextCSS({
    fontCSS: new FontCSS({
        fontWeight: "bold",
        fontSize: "20px",
        color: "white"
    })
});
