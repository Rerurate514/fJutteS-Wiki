import { TextCSS, FontCSS } from "fjuttes";

export const SUMMARY_TEXT_CSS_WHITE = new TextCSS({
    fontCSS: new FontCSS({
        fontWeight: "bold",
        fontSize: "20px",
        color: "white"
    })
});

export const SUMMARY_TEXT_CSS_BLACK = new TextCSS({
    fontCSS: new FontCSS({
        fontWeight: "bold",
        fontSize: "20px",
        color: "black"
    })
});
