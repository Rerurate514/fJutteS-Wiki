import { TextCSS, FontCSS } from "fjuttes";

export const SummaryText = new TextCSS({
    fontCSS: new FontCSS({
        fontWeight: "bold",
        fontSize: "20px",
        color: "white"
    })
});
